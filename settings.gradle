plugins {
    id "org.gradle.toolchains.foojay-resolver-convention" version "0.8.0"
}

rootProject.name = "claude-code-proxy-java"

// The routing vocabulary (:proxy-contracts), the IR types (:ir) and api's seam modules resolve as
// published jars through github-gradle, so only this repo's own two projects are included. Every
// module must name api at ONE coordinate: :ir exports api's seam types in HandlerCtx's public fields.

// :claude-code-proxy-plugin, the publishable *-proxy*.jar (ClaudeCodeProxyPlugin + AnthropicRateLimit).
include ":claude-code-proxy-plugin"
// :claude-code-proxy-teavm, TeaVM JS export of AnthropicRateLimit.synthCore.
include ":claude-code-proxy-teavm"
