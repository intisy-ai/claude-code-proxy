{
  "name": "claude-code-proxy",
  "version": "2.2.0",
  "description": "The Claude/Anthropic proxy layer (anthropicProfile) on top of the generic basekit proxy engine",
  "type": "module",
  "main": "dist/index.js",
  "license": "MIT",
  "author": "intisy",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/intisy-ai/claude-code-proxy.git"
  },
  "keywords": [
    "proxy",
    "routing",
    "anthropic",
    "claude",
    "intisy",
    "claude-code-proxy"
  ],
  "files": [
    "dist/",
    "plugin.json",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm run emit:teavm && npm run build:teavm && npx tsc && node build.mjs && npx esbuild src/front-door-plugin.ts --bundle --platform=node --format=esm --external:@intisy-ai/basekit/proxy --external:@intisy-ai/basekit/ir --external:@intisy-ai/anthropic-translator --outfile=dist/front-door-plugin.js",
    "build:teavm": "node node_modules/@intisy-ai/api/scripts/teavm-build.mjs --java-dir . --module :claude-code-proxy-teavm --out src/generated/anthropic-rate-limit.teavm.js",
    "test": "npm run build && vitest run",
    "prepublishOnly": "npm run build",
    "emit:teavm": "node node_modules/@intisy-ai/api/scripts/emit-dts.mjs --java-dir . --module :claude-code-proxy-teavm --out src/generated",
    "docs": "typedoc"
  },
  "dependencies": {
    "@intisy-ai/anthropic-translator": "^1.1.0",
    "@intisy-ai/basekit": "^4.0.0"
  },
  "devDependencies": {
    "@intisy-ai/api": "^1.6.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.21.5",
    "typedoc": "^0.28.20",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
